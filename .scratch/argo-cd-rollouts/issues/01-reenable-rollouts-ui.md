# Re-enable Rollouts UI

Status: needs-triage

`server.enableRolloutsUI` is `false` in `../apps/infra/argo-cd/envs/kind-olm/argocd-argocd.yaml` as a workaround for an upstream regression; catch up with upstream and re-enable once fixed.
