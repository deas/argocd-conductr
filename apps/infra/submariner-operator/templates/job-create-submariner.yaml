{{- if .Values.postInstallJob.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Release.Name }}-setup-submariner"
  namespace: "{{ .Release.Namespace }}"
  labels:
    heritage: {{ .Release.Service | quote }}
    release: {{ .Release.Name | quote }}
    chart: {{ template "submariner.chart" . }}
    app: {{ template "submariner.fullname" . }}
  annotations:
    # This is what defines this resource as a hook. Without this line, the
    # job is considered part of the release.
    "helm.sh/hook": post-install
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  template:
    metadata:
      name: "{{ .Release.Name }}-setup-submariner"
      labels:
        app: "{{ .Release.Name }}"
    spec:
      serviceAccountName: "{{ template "submariner.fullname" . }}-manager"
      restartPolicy: Never
      containers:
        - name: setup-submariner
          image: bitnami/kubectl
          command: ["/bin/sh"]
          args: ["/assets/create-submariner.sh", "/assets/submariner-template.yaml"]
          volumeMounts:
            - name: assets
              mountPath: /assets         # Specify the directory where the config map will be mounted
      volumes:
        - name: assets
          configMap:
            name: "{{ .Release.Name }}-templates"

{{- end }}
